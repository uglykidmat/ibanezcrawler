{% extends 'base.html.twig' %}

{% block body %}
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <div class="card bg-base-200 shadow-lg p-4">
        <h3 class="text-lg font-semibold">Total Guitars</h3>
        <p class="text-2xl font-bold">{{ total_guitars }}</p>
    </div>
    <div class="card bg-base-200 shadow-lg p-4">
        <h3 class="text-lg font-semibold">Different families</h3>
        <p class="text-2xl font-bold">{{totaldifferentfamilies}}</p>
    </div>
    {# <div class="card bg-base-200 shadow-lg p-4">
        <h3 class="text-lg font-semibold">Acoustic Guitars</h3>
        <p class="text-2xl font-bold">???</p>
    </div> #}
</div>
{% endblock %}
